jira ticket #

task 2 related to jetpack compose

/**
 * Task: Implement a simple Jetpack Compose UI for Mifos using Kotlin Multiplatform.
 * 
 * Description:
 * - Create a composable function that displays a list of Mifos clients.
 * - The data should be fetched from a shared Kotlin Multiplatform module (simulated here).
 * - The UI should be compatible with Android (Jetpack Compose).
 */

// Shared data model (Kotlin Multiplatform commonMain)
data class Client(val id: Int, val name: String)

// Simulated repository (would be in shared module)
object ClientRepository {
    fun getClients(): List<Client> = listOf(
        Client(1, "Alice"),
        Client(2, "Bob"),
        Client(3, "Charlie")
    )
}

// Jetpack Compose UI (Android-specific)
@Composable
fun ClientListScreen() {
    val clients = remember { ClientRepository.getClients() }
    Column {
        Text("Mifos Clients", style = MaterialTheme.typography.h6)
        clients.forEach { client ->
            Text("ID: ${client.id} - Name: ${client.name}")
        }
    }
}
